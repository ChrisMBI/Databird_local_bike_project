version: 2

models:
  - name: mrt_local_bike__customers
    description: '{{ doc("mrt_local_bike__customers") }}'
    columns:
      - name: order_id
        description: "Primary key, unique identifier for each order."
        tests:
          - unique
          - not_null